# Pensadero NODO — Reconocimiento facial
#
# Dependencias Python para el módulo de detección y reconocimiento facial
# (face_detector.py). Instalar dentro de un venv local del proyecto:
#
#   cd backend/python
#   python -m venv .venv
#   .venv\Scripts\activate    # Windows
#   pip install -r requirements.txt
#
# Usamos `onnxruntime-gpu`: en máquinas con NVIDIA CUDA aprovecha la GPU
# (~10x más rápido, 1s/imagen vs 8s en CPU). En máquinas sin GPU compatible
# cae a CPU automáticamente; el código de face_detector.py detecta los
# providers disponibles y los elige en orden CUDA → CPU.
#
# Si la instalación de onnxruntime-gpu falla en algún equipo (incompatibilidad
# de CUDA, drivers viejos, etc.), sustituir por `onnxruntime` a secas y todo
# seguirá funcionando, solo más lento.

insightface==0.7.3
onnxruntime-gpu>=1.18
numpy>=1.24
opencv-python>=4.8
pillow>=10.0

# SigLIP-2 (Google, 2024) — image+text embeddings multilingue en el mismo
# espacio de 768 dims. Sustituye a CLIP base + M-CLIP. Soporta español
# nativamente sin projection layers extra. ~600 MB modelo, ~1 GB VRAM.
torch>=2.6           # requerido por transformers>=5 (vulnerabilidad CVE-2025-32434)
transformers>=4.49   # SigLIP-2 anadido en 4.49; transformers 5.x funciona

# CUDA runtime libraries vía pip (no requiere instalar CUDA Toolkit de
# sistema, no requiere admin). face_detector.py registra estas carpetas
# en el PATH al arrancar para que Windows resuelva las dependencias
# transitivas de onnxruntime_providers_cuda.dll.
nvidia-cublas-cu12
nvidia-cuda-runtime-cu12
nvidia-cuda-nvrtc-cu12
nvidia-cudnn-cu12
nvidia-cufft-cu12
nvidia-curand-cu12
nvidia-cusparse-cu12
nvidia-cusolver-cu12
nvidia-cuda-cupti-cu12
